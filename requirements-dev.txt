# Test-only dependencies. Kept out of requirements.txt on purpose: the
# Dockerfile installs that file into the production image, and pytest has no
# business shipping to Azure.
-r requirements.txt

pytest==8.3.*
pytest-asyncio==0.24.*
